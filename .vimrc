" Habilitar el uso del mouse en modo normal y visual
set mouse=a

" Habilitar resaltado de sintaxis
syntax off

" Números de línea
" set number

" Deshabilitar la creación de archivos de respaldo
set nobackup
set nowritebackup
set noswapfile

" Indentación automática
set autoindent
set smartindent

" Habilitar la opción de copiar y pegar entre Vim y el portapapeles del sistema
set clipboard=unnamedplus

set showmode

" Establecer el ancho de tabulación en 4 espacios
set tabstop=4
" Establecer el ancho de tabulación cuando se usan espacios para la sangría
set shiftwidth=4

" Ajuste de tamaño de pestañas
set tabstop=4
set softtabstop=4
set shiftwidth=4
set expandtab

" Buscar sin distinguir entre mayúsculas y minúsculas si hay una mayúscula
set smartcase

" Habilitar autocompletado de palabras
set complete+=kspell

" Muestra la columna de la posición del cursor
set ruler

" Ignorar el caso al buscar
set ignorecase


" Habilitar resaltado de búsqueda
set hlsearch

" Habilitar búsqueda incremental mientras escribes
set incsearch

